# Option 1: Full URL (password must be URL-encoded if it has special chars like @ or !)
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Option 2: Separate vars (recommended - handles special chars automatically)
DB_HOST=db.xxxxx.supabase.co
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your-password-here
DB_NAME=postgres
